config NFC_ST21NFCD
	bool "ST ST21NFCD NFC SUPPORT"
	default n
	---help---
	  STMicroelectronics ST21NFCD core driver. It implements the chipset
	  HCI logic and hooks into the NFC kernel APIs. Physical layers will
	  register against it.
	  Say N if unsure.
